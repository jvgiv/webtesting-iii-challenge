// Test away!
import React from 'react'
import { render } from 'react-testing-library'
import Display from './Display'

describe('Display Page', () => {

    it('renders the page', () => {
        render(<Display />);
    })

    it('displays if gate is open/closed if its locked/unlocked', () =>{

    })

    it('displays if gate is locked/unlocked', () => {

    })

    it('displays closed if closed prop is true, open if otherwise', () => {

    })

    it("displays locked if locked prop is true", () => {

    })

    it('uses red-led class when locked or closed', () => {

    })

    it('uses green-led class when unlocked or open', () => {
        
    })
})
