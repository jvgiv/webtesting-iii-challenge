// Test away!
import React from 'react'
import { render, fireEvent } from 'react-testing-library'
import Controls from './Controls'

describe('Controls', () => {
    it('renders', () => {
        render(<Controls />)
    })
    

    it('provides buttons to toggle the closed and locked states', () => {

    })

    it('button text changes to reflect the state the door will be if clicked', () => {

    })

    it('closed toggle button is disabled if gate is closed', () => {

    })

    it('locked toggle is disabled if gate is open', () => {
        
    })
})